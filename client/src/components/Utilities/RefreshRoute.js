import React from 'react';
import Redirect from 'react-router-dom';

function RefreshRoute(props) {
    return (
        <Redirect 
            {...props}
            render 
        
        
        />
    )
}

export default RefreshRoute
